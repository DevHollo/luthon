class math <
   func add(a, b) <
      return a + b
   >
   func subtract(a, b) <
      return a - b
   >
   func multiply(a, b) <
      retrun a * b
   >
   func divide(a, b) <
      return a / b
   >
>